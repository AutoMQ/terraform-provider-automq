## 0.1.0 (Unreleased)

FEATURES:

- Add `automq_environment` lifecycle management.
- Default the AutoMQ API endpoint to `https://console.automq.cloud`.
- Route environment-owned resources through `/api/v1/environments/{envId}` and remove the legacy environment header.
